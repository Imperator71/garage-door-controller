name: Build & Push Docker Image

on:
  push:
    branches: [main]
    paths:
      - '_Docker-Database/**'
      - '.github/workflows/docker-publish.yml'

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: ./_Docker-Database
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/garage-fingerprint-registry:latest
            ghcr.io/${{ github.repository_owner }}/garage-fingerprint-registry:${{ github.sha }}
